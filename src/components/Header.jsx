import React from "react";

function Header(props) {
   return (
      <>
         {/* <head>
            <title>{props.title}</title>
         </head> */}
         <h1>{props.title  }</h1>
      </>
   );
}

export default Header;
